Book Agent Mainline Handoff
Last Updated: 2026-04-03 12:05:01 +0800

1. 当前主线

当前 run 是 Forge v2 的 `change_request` 接管，不再沿着旧的 runtime self-heal mainline 继续扩写。

新的真实主线是：

`高保真文档翻译底座`

它的第一阶段不是直接做高还原 EPUB/PDF 导出，而是先把下面三件事落成仓库真相：

- parse revision 持久化
- Canonical Document IR sidecar
- execution artifact 对 parse revision / canonical node 的 provenance 回指

2. 当前批次

- latest passing feature id: `F006`
- current_step: `batch-66_in_progress`
- active_batch: `batch-66`
- authoritative_batch_contract: `.forge/batches/batch-66.md`
- expected_report_path: `.forge/reports/batch-66-report.md`

3. 为什么这是当前 change_request

用户明确要求后续开发由 Forge v2 自动开发框架全权负责，并将目标切到高保真文档翻译架构。

因此当前最小 dependency-closed slice 不是 exporter 重写，而是：

- 让 parser 产物拥有 durable canonical truth
- 让后续 EPUB patch export / PDF risk-aware planning 有共同真源

4. 已完成的 batch-64

- parse revision ORM / sidecar 记录已经落地
- canonical IR 类型与 parse IR service 已落地
- parse IR repository 已落地
- block/sentence provenance 已接入 parse revision / canonical node
- targeted unittest 和 py_compile 已通过

5. 已完成的 batch-65

- `zh_epub` 文档级 source-preserving export 已落地
- 原始 EPUB archive / XHTML patch 路径已落地
- 目录、锚点、内部跳转和 archive 结构在 targeted test 中已保留
- batch-65 验证已经通过

6. batch-66 的目标

- 持久化 PDF page/zone extraction intent
- 把 extraction-mode risk reasons 写回 canonical truth
- 为后续 PDF 高保真导出打通 restartable parser truth

7. 当前 resume 基线

- `bash .forge/init.sh`
- smoke warning hygiene validated
- governance contract validated
- `tests.test_parse_ir`
- `tests.test_source_preserving_epub_export`

8. Forge v2 继续约束

- 仍然执行 continuation scan
- 仍然用 `mainline_required / mainline_adjacent / out_of_band` 处理 branch work
- 仍然要求 stop-legality 从 file truth 可审计
